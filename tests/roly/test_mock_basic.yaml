given:
  mock_tasks:
    - name: Create a temp folder
      mock: {}

test_cases:
  - name: "test_mock_basic"
    tasks:
      - name: Create a temp folder
        tempfile:
          state: directory
          suffix: build
